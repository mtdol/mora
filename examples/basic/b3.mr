fn printArgs {
    dec i;
    i <- 0;

    While i /= length argsG {
        putLn $ argsG ! i;
        i +< 1;
    }
}

dec argsG;

fn main args {
    argsG <- args;
    printArgs();
    return 0;
}
